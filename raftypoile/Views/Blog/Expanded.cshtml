@model Post
@if (Model.IdPicture != 0)
{
    <img class="expanded-pic" alt="@Model.IdPictureNavigation.AlternativeText" src="@Model.IdPictureNavigation.SourceHeader">
}
<div class="expanded-view">
    @Html.Raw(Model.Body)
</div>